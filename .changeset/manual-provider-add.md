---
"@moonshot-ai/kimi-code": minor
---

Add a manual add flow to /provider for custom endpoints. Run /provider, choose "Manual (custom endpoint)", and enter the base URL, API key, model, and thinking support to add a self-hosted provider without editing config.toml.
